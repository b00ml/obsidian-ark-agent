# Ark application boundary

Ark is a TypeScript Obsidian plugin and its implementation owner remains
[`../../ark/`](../../ark/). This directory is a migration marker only; it does
not duplicate plugin source or change the existing `ark/` build and deployment
commands.
