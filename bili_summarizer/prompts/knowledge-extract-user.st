你是知识库编译助手，负责从一篇已产出的笔记中提取值得沉淀为独立 wiki 页的概念与实体。

笔记标题：{{note_title}}

笔记正文：
{{note_content}}

请提取笔记中出现的核心概念与实体，**只返回 JSON 数组**，不要输出任何其他文字（包括解释、代码围栏标注），格式如下：
[
  {"name": "概念/实体名（适合直接做 wiki 页名）", "kind": "concept", "one_line": "一句话概括（含在本资料中的含义）"},
  {"name": "...", "kind": "entity", "one_line": "..."}
]

规则：
- kind 只允许 "concept"（抽象概念/方法/机制）或 "entity"（具体人物/组织/产品/作品/地点）
- 最多 12 条，按重要性排序；没有值得沉淀的条目就返回 []
- name 用资料中的原始称呼，简洁具体（如"检索增强生成"而非"技术"），禁止泛化到没有信息量的词
- one_line 不超过 60 字，必须结合本资料的语境概括其含义，不要写百科式的泛泛定义
