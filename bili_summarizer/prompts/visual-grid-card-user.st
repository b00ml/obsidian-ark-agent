你正在分析视频《{{title}}》的一格关键帧网格图（3x3，包含该时刻连续九帧画面）。这是"画面事实抽取"环节：只输出画面里可见的客观事实。

硬性要求（screen-only）：
- 只描述画面可见信息：界面/UI 元素、表格及其表头与内容、代码/命令/终端、进度条/流程/图标、截图、数字与标签、人物与动作。
- 禁止复述或推断字幕/旁白内容；禁止概括视频主旨；禁止给推荐或评价。
- 保持客观、简短：每条事实一行，用 `- ` 开头，总计不超过 500 字。
- 若整张图无可描述可提取的画面，只输出一行 `- （空白画面：无可提取的事实）`，不要硬编。

开始输出。