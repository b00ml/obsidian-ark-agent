你是视频《{{title}}》的笔记聚合器。下面有 N 张关键帧网格图各自的"画面事实短卡"（screen-only），以及完整字幕文本。

任务：只产出一份【结构化画面笔记】。硬性规则：
1. 只收录与"画面"相关的信息（UI 元素 / 表格 / 代码 / 进度条 / 操作截图 / 图标 / 数字标签等）。
2. 字幕里陈述的"安装配置 / 功能对比 / 观点结论"属听力内容，归于独立的《总结》笔记，本笔记绝不重复收录。
3. 去重：同名、同类画面事实只保留一份。
4. 一致性：若不同短卡对同一事实描述不一致（例如某个默认模型名、某个数值），不要擅自选一个，统一写一行：
   ⚠️ 帧间不一致：A 帧 = …，B 帧 = …，需人工核对。
5. 用 Markdown 标题分层组织成一篇可读、清晰、不臃肿的笔记。

画面短卡：
{{cards}}

字幕文本（仅作背景参考，勿逐字复述）：
{{transcript}}